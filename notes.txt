JSNI
    JSO -> 
			extends JavaScriptObject,
          	no instance variables 
          	constructor needs to be protected,
          	nothing i.e no code inside the constructor,
          	return should use this.
    JSO -> Without use of WrapperUtils an inner jso gets created for each of the extended JSO objects

JSNI -> troubles with Generic when invoking code from JSNI code blocks.

TODO:
Generalize DataProvider - PieChartBrokenSlices
Generalize or eliminate wrap and unwrap step
Color - currently set as string
Confirm using of int/double/boolean instead of Integer/Double/Boolean. 